name: Ansible Lint

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Lint Ansible
      uses: ansible/ansible-lint-atcion@f8608f5
      with:
        targets: "./"
        args: "-q"